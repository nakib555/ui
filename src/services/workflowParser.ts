
/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

// This file is intentionally left blank.
// The workflow parsing logic has been moved to the backend in `backend/utils/workflowParser.ts`
// to improve frontend performance and handle streaming updates more efficiently.
// The types `WorkflowNodeData` and `ParsedWorkflow` are now defined in `src/types/workflow.ts`
// (which should be synchronized with `backend/types.ts`).